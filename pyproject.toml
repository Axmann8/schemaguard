[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "schemaguard"
version = "1.4.2"
description = "Lightweight schema-validation guard for data pipelines."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"

[project.urls]
Homepage = "https://github.com/Axmann8/schemaguard"
